# Development Mode Configuration
# ================================
# Set to 'true' to enable mock services - no backend server required!
# This allows you to develop the UI without running the Python backend.
# 
# IMPORTANT: In production, either:
# - Remove this file (.env.local)
# - Set NEXT_PUBLIC_DEV_MODE to 'false'
# - Don't set this variable at all
#
# Default: false (uses real backend)
NEXT_PUBLIC_DEV_MODE=true

# Dev Mode Auto-Login (optional)
# ================================
# These credentials are only used when DEV_MODE is enabled
# They allow automatic login without entering credentials
NEXT_PUBLIC_DEV_USER=dev@skygen.local
NEXT_PUBLIC_DEV_PASSWORD=dev123

# Production Backend URL (when DEV_MODE is disabled)
# ================================
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# NEXT_PUBLIC_WS_URL=ws://localhost:8000
